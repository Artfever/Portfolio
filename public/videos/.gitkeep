# Drop compressed project demo videos here (e.g. placeholder.mp4, project-one.mp4).
# Hover-preview loops: target 5-15MB. Full demo clips: target ~30-50MB.
# Compress with: ffmpeg -i input.mp4 -vcodec h264 -crf 26 -preset slow output.mp4
